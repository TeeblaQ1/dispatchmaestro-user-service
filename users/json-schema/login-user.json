{
    "type": "object",
    "properties": {
      "email_address": {
        "type": "string",
        "format": "email"
      },
      "password": {
        "type": "string",
        "minLength": 6,
        "maxLength": 20
      },
      "entity_type": {
        "type": "string",
        "enum": ["client", "partner"]
      }
    },
    "additionalProperties": false,
    "required": ["email_address", "password", "entity_type"]
}